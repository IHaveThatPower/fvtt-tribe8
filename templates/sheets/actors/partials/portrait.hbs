<div class="portrait" data-uuid="{{document.uuid}}" data-edit="img">
	<div class="content">
	{{#if portrait}}
		<img src="{{portrait}}" alt="{{document.name}}">
	{{else}}
	Your face here
	{{/if}}
	</div>
</div>